/**
 * The interface of the renderer
 * We want every one to implement the renderBoard method
 */
interface Renderer {
    /**
     * A method that render the board
     * @param board to render
     */
    void renderBoard(Board board);
}
