/**
 * A class of a void renderer (doesn't show anything)
 */
public class VoidRenderer implements Renderer{
    /**
     * constructor to a void renderer (doesn't show anything)
     * @param board to render
     */
    @Override
    public void renderBoard(Board board) {

    }
}
